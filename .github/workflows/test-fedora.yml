name: CI Arnika Labs
on: [push]
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Check Context
        run: echo "Ce job tourne sur le runner partagé de Arnika Labs !"